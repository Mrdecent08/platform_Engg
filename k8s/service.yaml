apiVersion: v1
kind: Service
metadata:
  name: kor-service
  namespace: ar
spec:
  type: NodePort
  selector:
    app: kor-application
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30228