apiVersion: apps/v1
kind: Deployment
metadata:
  name: pod-waiting1-deployment
  namespace: remediation
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pod-waiting
  template:
    metadata:
      labels:
        app: pod-waiting
    spec:
      containers:
      - name: nginx
        image: nginx:0.0.100
        ports:
        - containerPort: 8080
