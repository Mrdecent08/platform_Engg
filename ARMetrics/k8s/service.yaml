apiVersion: v1
kind: Service
metadata:
  name: auto-remediation-service
  namespace: ar
spec:
  type: NodePort
  selector:
    app: auto-remediation-application
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30128